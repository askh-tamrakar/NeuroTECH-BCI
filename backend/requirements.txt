# ─────────────────────────────────────────────────────────────────
# NeuroTECH-BCI Backend Requirements
# Python >= 3.10 required (union type syntax used in eeg_lda_trainer)
# ─────────────────────────────────────────────────────────────────

# ── Web Server & API ─────────────────────────────────────────────
flask>=3.0.0
flask-socketio>=5.3.0
flask-cors>=4.0.0
python-socketio>=5.5.0
eventlet>=0.35.0       # Async mode for Flask-SocketIO (WebSocket support)
simple_websocket>=0.9.0
requests>=2.28.0       # HTTP client used by local auth sync (auth_routes.py)
# fastapi>=0.115.0    # Not used by current server (Flask-based)
# uvicorn>=0.32.0     # Not used by current server

# ── Scientific Computing ─────────────────────────────────────────
numpy>=1.26.0,<3.0.0
scipy>=1.12.0
pandas>=2.0.0

# ── Machine Learning ─────────────────────────────────────────────
scikit-learn>=1.4.0
joblib>=1.3.0

# ── BCI / Signal Acquisition ─────────────────────────────────────
pylsl>=1.16.2          # Lab Streaming Layer (stream_manager, filter_router, feature router)
pyserial>=3.5          # Serial port reading (acquisition_app, serial_reader)

# ── Signal Visualization (acquisition_app GUI) ───────────────────
matplotlib>=3.8.0

# ── HID Actuation ────────────────────────────────────────────────
pynput>=1.7.7          # Keyboard/mouse control (hid_controller)

# ── Optional: NeuroBench Signal Simulator (dev tool only) ────────
# Uncomment the lines below if you use src/utils/neurobench.py
# PySide6>=6.6.0
# pyqtgraph>=0.13.0
# brainflow>=5.12.0
